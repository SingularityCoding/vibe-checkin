import type { LearningRecord } from '../../domain/learning-record'

export const collectSuggestedTags = (_records: readonly LearningRecord[]): string[] => []

export const normalizeSelectedTags = (_tags: readonly string[]): string[] => []

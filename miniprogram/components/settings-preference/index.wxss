:host {
  display: block;
}

.heading {
  margin-bottom: 20rpx;
  color: var(--vc-color-ink);
  font-size: 34rpx;
  font-weight: 600;
}

.duration-control {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.duration-unit {
  color: var(--vc-color-text-secondary);
  font-size: 24rpx;
}

.status-note {
  margin-top: 16rpx;
}
